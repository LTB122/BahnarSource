import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  iconBorder: {
    backgroundColor: '#fff',
    borderRadius: 20,
    borderWidth: 2,
    borderStyle: 'solid',
    borderColor: '#E1E4F2',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
